# Risk Budget Adherence Report

Validates that aggregate raw usages do not violate `max_risk_budget_usage_fraction`.
Evaluates max usage, average usage, and the number of limit breaches. Outputs are diagnostic boundaries, disconnected from actual capital allocations.
