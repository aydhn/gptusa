# Promotion Evidence Refresh
Refreshes and analyzes promotion evidence required for observer governance.

## Purpose
Ensures previous and current governance requirements are fresh and available.

## CLI Usage
```bash
python -m usa_signal_bot observer-evidence-collect --write
python -m usa_signal_bot observer-evidence-freshness --write
python -m usa_signal_bot observer-evidence-gaps --write
```
