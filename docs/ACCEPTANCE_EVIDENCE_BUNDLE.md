# Acceptance Evidence Bundle

Collects expected evidence items mapped to dry run steps.
Requires all items to be available locally and marked as `read_only`.
Ensures the system produces verifiable outputs for each acceptance area safely.
