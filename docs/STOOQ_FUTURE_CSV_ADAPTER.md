# Stooq Future CSV Adapter

Provides a future-ready wrapper for stooq.com CSV downloads via pandas.
Real URL fetching is restricted and defaulted to `False` (`NETWORK_GUARDED_DISABLED`).
No HTML scraping or parsing is included.
