# Deterministic & Heuristic Regime Labeling

Uses simple priority and threshold approaches:
- Top candidate labeling based on largest score gap.
- Threshold priority labeling.
- Score gap confidence proxy.
- Fallback/mixed labels.

No "buy" or "sell" outputs. Labels do not trigger market entry/exit.
