# Data Layer Acceptance Rehearsal

Rehearsal executes scenarios in a strict metadata-only, dry-run mode.
It tests provider routes, cache lookup, scoring, and output contracts without any network execution or mutation.
