# Strategy Diagnostics

Classifies strategy health into HEALTHY, WATCH, DEGRADED, FAILING, INSUFFICIENT_DATA. Does not automatically toggle strategies off.

CLI commands:
python -m usa_signal_bot strategy-diagnostics --write
