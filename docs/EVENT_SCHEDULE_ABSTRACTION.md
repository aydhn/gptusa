# EVENT SCHEDULE ABSTRACTION

Abstracts market events into `UnifiedMarketEvent`.
Contains `EventSchedule` and `EventScheduleIndex`.
Handles deduplication, timezone normalization, and importance tagging skeletons.
Strictly "research context only".
