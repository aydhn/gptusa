# Phase 156: Equal, Score, and Risk Budget Optimizer Prototypes

Details the implementation of the equal baseline, score maximizing, and risk budget optimizer models. The output produces solely `sandbox_optimizer_weight`. No `target_weight` output is permitted.
