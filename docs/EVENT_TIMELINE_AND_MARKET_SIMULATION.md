# Event Timeline and Market Simulation

Prevents lookahead bias by modeling the availability of features relative to the bar execution logic.
Next-bar execution is the default assumption. Same-bar execution is heavily restricted or disallowed.
