# Non-Executing Observer Governance
Evaluates observer candidate without triggering active paper trading or broker execution.

## Purpose
Ensures safe governance promotion based strictly on non-executing metrics.

## CLI Usage
```bash
python -m usa_signal_bot observer-governance-gates --write
python -m usa_signal_bot observer-governance-decision --write
```
