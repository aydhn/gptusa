Phase 87: Guarded Paper-Mode Admission Review, Approval-Ledger Reconciliation, and Final No-Write Transition Checkpoint

This phase successfully implements the guarded paper-mode admission review process as requested.
The system now safely evaluates Phase 86 dry admission output, reconciles the human approval ledger, and prepares the final no-write transition checkpoint.

Features implemented:
- Added necessary enums and exceptions.
- Added configurations to core config schema and `default.yaml`.
- Created robust `PaperModeAdmissionReview`, `LedgerReconciliationReport`, `AdmissionEvidenceSeal`, and `FinalNoWriteTransitionCheckpoint` models.
- Established dry admission ingestion parsing and eligibility checks.
- Constructed rigorous admission gates.
- Built an approval ledger reconciliation engine.
- Ensured zero continuity breaks for strict no-write constraints.
- Generated immutable and deterministic evidence seals.
- Created local metadata-only decision-making engine.
- Implemented multi-layered safety validators handling risk flags for mutation and active executions.
- Generated audit trails and full admission reports.
- Adapted into data quality evaluator and metric collectors.
- Supplied robust notification formatting for dry-run.
- Fully wired health checks and CLI functionalities with exhaustive unit tests verifying behavior.
- Documented key limitations, safety guards, and phase sum-ups within `/docs/`.

Note: As heavily enforced, this constitutes a local, metadata-only evaluation and explicitly does not perform any active broker orders, API invocations or paper mode modifications.
