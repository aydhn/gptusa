feat: Implement Phase 100 - Final Pre-Paper Handoff Freeze Gate

This commit implements the final phase of the MVP local-offline pre-paper pipeline.

- Adds new models and enums for handoff freeze gates and simulator evidence freeze.
- Implements sandbox runtime admission blocker replay logic, verifying that all previous events effectively block sandbox access.
- Implements simulator evidence freeze logic, bundling all required evidence traces into a hashed, sealed bundle.
- Implements final handoff freeze gate validation rules and safety checks, explicitly enforcing non-execution policies (e.g. no active paper enable, no broker API connections, no Telegram real sends, no config writes, no paper order mutations).
- Adds audit trailing, reporting, and file storage for handoff artifacts.
- Adds adapters for ingesting earlier dossier states (simulator, dry-admission, paper runtime).
- Updates CLI, quality evaluation, observability metrics, and config schemas.
- Adds comprehensive documentation explicitly detailing the non-execution, read-only scope of this release.
- Fixes passing of all isolated new unit tests.
