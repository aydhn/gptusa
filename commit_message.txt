feat: implement phase 123 factor explainability and report layer

Implemented the factor explainability and feature attribution engines as
specified in Phase 123. This includes:
- Added enum values and dataclasses for explainability contexts, report
  sections, and feature attributions ensuring deterministic structure.
- Developed factor validation ingestion routines rejecting records that
  expose broker permissions or paper state mutations.
- Implemented file artifact loaders incorporating safety constraints (no
  directory traversal, forbidding 'buy', 'sell' columns).
- Created deterministic feature attribution logic using normalization
  without machine learning techniques.
- Drafted interpretation summary builders conforming to non-execution
  disclaimer standards.
- Designed comprehensive QA report validators that actively scan text for
  trade signals ("buy signal", "garanti kâr") and reject transition flags.
- Built a markdown and JSON rendering layer, persisting explainability
  metadata effectively to local structures.
- Added comprehensive unit tests reflecting the required safe-language
  and zero-mutation operational policies.
