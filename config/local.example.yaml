# Copy this file to local.yaml for your local overrides.
# Do NOT commit local.yaml to version control.
# Environment secrets like Telegram tokens should be passed via env vars,
# not hardcoded here.

telegram:
  enabled: false
  bot_token_env: "TELEGRAM_BOT_TOKEN"
  chat_id_env: "TELEGRAM_CHAT_ID"

risk:
  max_position_pct: 0.05
  max_total_exposure_pct: 0.50

universe:
  max_symbols_per_scan: 100
  allow_inactive_symbols: true

data_quality:
  enabled: true
  allow_zero_volume: true
  allow_warnings: true
  repair_enabled: true
  drop_invalid_price_bars: true
  drop_duplicate_bars: true
  fill_missing_volume_with_zero: true
  max_allowed_warning_ratio: 0.20
  block_on_errors: true

cache_refresh:
  enabled: true
  default_ttl_seconds: 86400
  force_refresh_default: false
  validate_cache_before_use: true
  repair_cache_before_use: true
  max_cache_age_days_daily: 3
  max_cache_age_days_intraday: 1

multi_timeframe:
  enabled: true
  default_timeframes:
    - "1d"
    - "1h"
    - "15m"
  primary_timeframe: "1d"
  confirmation_timeframes:
    - "1h"
  intraday_timeframes:
    - "15m"
  max_timeframes_per_run: 4
  max_symbols_per_multitimeframe_run: 50

data_readiness:
  enabled: true
  min_ready_pair_ratio: 0.70
  min_symbol_coverage_ratio: 0.70
  require_primary_timeframe: true
  allow_partial_intraday: true
  max_error_count: 0
  max_warning_ratio: 0.30
  write_reports: true

momentum_features:
  enabled: true
  default_indicator_set: "basic_momentum"


volatility_features:
  enabled: true
  default_indicator_set: "basic_volatility"
  available_indicator_sets:
    - "basic_volatility"
    - "band_volatility"
    - "channel_volatility"
    - "compression_volatility"
    - "full_volatility"
  default_atr_window: 14
  default_bollinger_window: 20
  default_bollinger_std: 2.0
  default_keltner_ema_window: 20
  default_keltner_atr_window: 10
  default_keltner_multiplier: 2.0
  default_donchian_window: 20
  default_rolling_volatility_window: 20
  default_reference_window: 100
  max_window: 1000
  allow_partial_volatility_features: true
  fail_on_negative_volatility: true

strategies:
  enabled: true

signals:
  enabled: true

signal_scoring:
  enabled: true
  min_score: 0.0
  max_score: 100.0
  base_score: 50.0
  confidence_weight: 25.0
  reason_quality_weight: 15.0
  feature_snapshot_weight: 10.0
  risk_penalty_weight: 20.0
  max_allowed_score_without_backtest: 70.0
  overconfidence_penalty: 15.0
  min_score_for_review: 40.0

signal_quality:
  enabled: true
  min_confidence_for_review: 0.25
  min_score_for_review: 40.0
  reject_missing_reasons: true
  reject_missing_feature_snapshot: true
  reject_expired_signals: true
  overconfidence_warning_threshold: 0.70
  max_rejected_ratio_warning: 0.80
  allow_empty_signal_list: true

confluence:
  enabled: true
  default_aggregation_mode: "by_symbol_timeframe"
  min_signals_for_confluence: 2
  conflict_penalty: 25.0
  strong_threshold: 70.0
  moderate_threshold: 50.0
  weak_threshold: 25.0
  write_confluence_reports: true
