# Universe Presets

Preset files are intended as starting points for building small universes.
- They do **not** represent the entirety of the US stock market.
- To create a comprehensive universe, users should import their own CSV files into the `imports` directory.
- Presets are useful for tests and small initial scans.
