# User Imports

Users can place wide universe CSV files here to build custom universes.
- File standard is the same as `WATCHLIST_STANDARD`.
- Local files only, URLs are not permitted.
- Use the `universe-import` CLI command to validate and normalize new files.
- For large universes, run validation before expanding.
