# Universe Readiness

Universe readiness gate reports are stored here.
This directory contains the final outputs of the active universe pipeline, most notably the `eligible_symbols.csv` and `eligible_symbols.txt` lists.
In future phases, the feature engine and trading logic will only process symbols found in these eligible lists.
