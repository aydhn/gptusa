# Universe Data Runs

Active universe data run metadata files are stored here.
For each run, a directory is created to store the metadata, active resolution, coverage, readiness, and gate reports.
These files preserve the execution history and provide the foundation for resume-friendly pipelines.
